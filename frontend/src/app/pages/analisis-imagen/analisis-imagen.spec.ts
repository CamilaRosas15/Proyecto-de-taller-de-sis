import { AnalisisImagenComponent } from './analisis-imagen';

describe('AnalisisImagenComponent', () => {
  let component: AnalisisImagenComponent;

  beforeEach(async () => {
    // Aquí puedes configurar tu entorno de prueba si es necesario
  });

  it('should create', () => {
    component = new AnalisisImagenComponent(null as any);
    expect(component).toBeTruthy();
  });
});
